# spark — 奇想录

没走通的念头(`items/<念头>/{meta.json, spark.md}`),一句话也能存。

## 必守规则

- **低摩擦是功能**:无必填小节,md 一行也行;别把门禁加严——想法蒸发比想法粗糙可怕。
- **毕业制**:走通 → `graduated` + `graduated_to` 真实路径(门禁强制);放弃 → `dropped` + 一行死因,尸体留着。
- 想到重复的点子,先查这里——可能早有尸体或早毕业了。
- HTML 为生成物,改完 meta 跑 `python3 scripts/build-index.py`。
